let lista = ['Pedro', 'Bruno', 'Fernando']

lista.push('Lucas') //Isere no final
lista.unshift('Anderson') // Insero no começo

lista.pop('') // Remove no final
lista.shift() // remove no começo
lista.splice(1, 1)

console.log(lista)
